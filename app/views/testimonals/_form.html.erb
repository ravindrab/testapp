<%= form_for(@testimonal) do |f| %>
  <% if @testimonal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testimonal.errors.count, "error") %> prohibited this testimonal from being saved:</h2>

      <ul>
      <% @testimonal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
